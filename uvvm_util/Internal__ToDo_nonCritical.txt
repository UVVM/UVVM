
List of issues to fix, add, etc. for UVVM Utility Library

General:


Methods:
-- TODO          : Shall we add random(unsigned/signed) ?
--                 -        u := random(min_value(u), max_value(u));
--                 - and/or u := random(u'length);

Pot. new methods
=======================

clock_gen (single clock, period [duty_cycle Hi)

clock_gen (ssecond single clock, derived from clock, relation....)

pulse ()   For both single cycle pusles and reset

all the RTL and other methods from IEEE_proposed  MARK in doc.

Log output of check_value should be changed
  from check_value() => OK, for std_logic 0. <msg>
  to   check_value() => SL'0', OK. <msg>

Allow indents (LOG none, all others N default 0)

Coordinate methods with IEEE Proposed
- Internal methods are not critical for first release, but good to include in overview later
  find_rightmost|leftmost
  minimum/maximum
  *_reduce

User should not see object orientation!?

Add reset method on protected types  (to allow TB reset???)


May be it would make sense to use protected ONLY when possible to modify from multiple places
(e.g. for alert counters, but not for enable|disable_log_msg())

Legg log header til som signal til waveview

  --------------------------------------------------------------------------------
  -- await_value
  --------------------------------------------------------------------------------
  -- Potential improvements
  --  - Adding an option that the signal must last for more than one delta cycle
  --    or a specified time
  --  - Adding a "AS_IS" option that does not allow the signal to change to other values
  --    before it changes to the expected value
  --
  -- The input signal is allowed to change to other values before ending up on the expected value,
  -- as long as it changes to the expected value within the time window (min_time to max_time).

  -- Wait for target = expected or timeout after max_time.
  -- Then check if (and when) the value changed to the expected


Do something about the comments (***) in the following procedures (comments were removed prior to release)
---------------------------------------------------------------------------------------------------------
  At least check if relevant to modify.

  procedure set_alert_file_name(
    constant file_name   : in string := C_ALERT_FILE_NAME
    ) is
    variable v_file_open_status: file_open_status;
  begin
    if not protected_alert_file_name_is_set.get(VOID) then
      protected_alert_file_name_is_set.set(true);
      file_close(ALERT_FILE);
      file_open(v_file_open_status, ALERT_FILE, file_name, write_mode);
      check_file_open_status(v_file_open_status, file_name);
      report "alert file name set: " & file_name; -- **** log???
    else
      report "alert file name already set"; -- **** tb_warning
    end if;
  end;

  procedure set_log_file_name(
    constant file_name   : in string := C_LOG_FILE_NAME
    ) is
    variable v_file_open_status: file_open_status;
  begin
    if not protected_log_file_name_is_set.get(VOID) then
      protected_log_file_name_is_set.set(true);
      file_close(LOG_FILE);
      file_open(v_file_open_status, LOG_FILE, file_name, write_mode);
      check_file_open_status(v_file_open_status, file_name);
      report "log file name set: " & file_name; -- **** log???
    else
      report "log file name already set"; -- **** tb_warning
    end if;
  end;



Check out the following removed comments from methods_pkg.
--------------------------------------------------------------------------
  Was located immediately after section heading "Non time consuming checks"

  -- Comments:
  -- 1. Do not want do double the number of procedures with overloads for common ID panel.
  --    Hence use default parameter (and thus a copy-procedure since protected type (access))
  -- 2. Problem: Kan ikke ta inn som t_msg_id_panel ettersom det skal videre inn i log-rutina.
  --    Kunne da kopiert tilbake i en protected type før log() kalles, men det blir mye kopiering
  --    Kunne også endret log() slik at den alltid bare kopierer fra protected.
  --    Dersom log også hadde en overload for t_msg_id_panel....
  --    MEN check_value må kunne kalles med andre ID paneler. Er det OK å kalle med .copy
  --    ==> Ja - i hvert fall foreløpig
  -- 3. Hva med kompatibilitet med Digitas. Må være kompatibelt også med gammel kode som kaller
  --    check_value med non-protected msg_id_panel


